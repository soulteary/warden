# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

*.tar

coverage.*

# 配置文件（可能包含敏感信息，如 Redis 密码等）
# 注意：请使用 config.example.yaml 作为模板，不要提交实际配置文件
config.yaml
config.yml
*.local.yaml
*.local.yml
*.local.json

# 用户数据文件（可能包含敏感信息，如用户手机号、邮箱等）
# 注意：请使用 data.example.json 作为模板，不要提交实际数据文件
data.json
*.local.data.json

# 环境变量文件（可能包含敏感信息，如 API keys、密码等）
# 注意：请使用 .env.example 作为模板，不要提交实际 .env 文件
.env
.env.local
.env.*.local