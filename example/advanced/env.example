# Warden 复杂示例环境变量配置
# 复制此文件为 .env 并修改相应值
# 使用方式: cp env.example .env

# ============================================
# 服务配置
# ============================================

# Warden 服务端口
PORT=8081

# ============================================
# Redis 配置
# ============================================

# Redis 地址（Docker Compose 中使用服务名）
REDIS=warden-redis:6379

# Redis 密码（可选，建议设置）
# REDIS_PASSWORD=your-redis-password

# ============================================
# 远程 API 配置
# ============================================

# 远程配置 API URL
CONFIG=http://mock-api:8080/api/users

# 远程配置 API 认证密钥
KEY=Bearer mock-token

# ============================================
# 任务配置
# ============================================

# 定时任务执行间隔（秒）
INTERVAL=10

# ============================================
# 运行模式
# ============================================

# 数据合并模式
# 可选值: DEFAULT, REMOTE_FIRST, LOCAL_FIRST, ONLY_REMOTE, ONLY_LOCAL
#         REMOTE_FIRST_ALLOW_REMOTE_FAILED, LOCAL_FIRST_ALLOW_REMOTE_FAILED
MODE=DEFAULT

# ============================================
# 安全配置
# ============================================

# API Key（用于 API 认证，生产环境必须设置）
API_KEY=your-secret-api-key-here

# ============================================
# HTTP 客户端配置
# ============================================

# HTTP 请求超时时间（秒）
HTTP_TIMEOUT=5

# HTTP 最大空闲连接数
HTTP_MAX_IDLE_CONNS=100

# 是否跳过 TLS 证书验证（仅用于开发环境）
# HTTP_INSECURE_TLS=false

# ============================================
# 日志配置
# ============================================

# 日志级别（可选，默认: info）
# 可选值: trace, debug, info, warn, error, fatal, panic
# LOG_LEVEL=info

# ============================================
# Mock API 配置（可选）
# ============================================

# Mock API 端口
# MOCK_API_PORT=8080

